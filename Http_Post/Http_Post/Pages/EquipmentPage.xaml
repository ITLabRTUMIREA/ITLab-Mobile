<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:Controls="clr-namespace:Http_Post.Controls"
             xmlns:img="clr-namespace:Http_Post.Images"
             xmlns:svg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             x:Class="Http_Post.Pages.EquipmentPage"
             Padding="5"
             Icon="Usb1.png">
    <AbsoluteLayout>
        <Grid AbsoluteLayout.LayoutBounds="0,0,1,1"
              AbsoluteLayout.LayoutFlags="All">
            <Grid.Margin>
                <OnPlatform x:TypeArguments="Thickness" 
                        iOS="5,30,0,0"
                        Default="5,10,0,0"/>
            </Grid.Margin>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Controls:ButtonsArrows x:Name="frameType"
                                      tapped="frameType_Clicked"
                                      Grid.Row="0"
                                      Grid.Column="0"/>
            <Controls:ButtonsArrows x:Name="frameOwner"
                                      tapped="frameOwner_Clicked"
                                      Grid.Row="0"
                                      Grid.Column="1"/>
            <Controls:ButtonsArrows x:Name="frameNumber"
                                      tapped="frameNumber_Clicked"
                                      Grid.Row="0"
                                      Grid.Column="2"/>
            <ListView x:Name="listView"
                      SeparatorVisibility="None"
                      IsPullToRefreshEnabled="True"
                      ItemTapped="listView_ItemTapped"
                      Grid.ColumnSpan="3"
                      Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Controls:EquipmentView/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Controls:CircleButton x:Name="btnCreate"
                               BtnTapped="btnCreate_Clicked"
                               IsVisible="False"
                               ImageSource="CreatePlus"
                               AbsoluteLayout.LayoutBounds="0.95,0.97,60,60"
                               AbsoluteLayout.LayoutFlags="PositionProportional"/>
        <Controls:CircleButton ImageSource="Refresh"
                               BtnTapped="btnRefresh_BtnTapped"
                               AbsoluteLayout.LayoutBounds="0.95,0.05,60,60"
                               AbsoluteLayout.LayoutFlags="PositionProportional">
            <Controls:CircleButton.IsVisible>
                <OnPlatform UWP="True"
                            Default="False"/>
            </Controls:CircleButton.IsVisible>
        </Controls:CircleButton>
    </AbsoluteLayout>
</ContentPage>