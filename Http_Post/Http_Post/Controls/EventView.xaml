<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Http_Post.Controls.EventView">
    <StackLayout>
            <StackLayout x:Name="Non_Phone">
                <!-- Layout for Computer or Tablet -->

                <StackLayout Orientation="Horizontal">
                    <Label x:Name="lblTitle" Text="{Binding Title}"/>
                    <!-- Label for Title -->
                    <StackLayout Orientation="Vertical">
                        <Label Text="Participants:"/>
                        <!-- Label and ProgBar for how many you need more -->
                    <ProgressBar x:Name="progBar" Progress="{Binding CustomProg}"/>
                    </StackLayout>
                </StackLayout>
                <StackLayout Orientation="Vertical">
                    <Label x:Name="lblDescription" Text="{Binding Description}}"/>
                    <!-- Label for Description -->

                    <StackLayout Orientation="Horizontal">
                        <!-- Time Start + Time End -->
                        <Label x:Name="lblTimeStart" Text="{Binding BeginTime}"/>
                        <Label x:Name="lblTimeEnd"/>
                    </StackLayout>

                    <StackLayout Orientation="Horizontal">
                        <Button Text="BtnGo" Clicked="BtnGo_Clicked"/>
                        <Button Text="BtnWontGo" Clicked="BtnWontGo_Clicked"/>
                    </StackLayout>

                </StackLayout>

            </StackLayout>

            <StackLayout x:Name="Phone">
                <!-- Layout for Phones -->
                <Label Text="GG WP! It's a DISASTAH ! ! !"/>
            </StackLayout>
        </StackLayout>
</ViewCell>